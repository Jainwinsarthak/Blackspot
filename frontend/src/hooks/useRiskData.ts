import {useEffect,useState} from 'react'; import {fetchSegments,fetchSummary} from '../utils/api'; import type {Condition,Segment,Summary} from '../types'; export function useRiskData(c:Condition){const [segments,setSegments]=useState<Segment[]>([]),[summary,setSummary]=useState<Summary|null>(null),[loading,setLoading]=useState(true);useEffect(()=>{setLoading(true);Promise.all([fetchSegments(c),fetchSummary()]).then(([s,x])=>{setSegments(s);setSummary(x)}).finally(()=>setLoading(false))},[c.weather,c.time,c.festival]);return {segments,summary,loading}}
