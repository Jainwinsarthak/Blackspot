import {useState} from 'react'; import type {Condition} from '../types'; export function useConditionSimulator(){const [conditions,setConditions]=useState<Condition>({weather:'clear',time:'day',festival:false});return {conditions,setWeather:(weather:Condition['weather'])=>setConditions(c=>({...c,weather})),toggleNight:()=>setConditions(c=>({...c,time:c.time==='night'?'day':'night'})),toggleFestival:()=>setConditions(c=>({...c,festival:!c.festival}))}}
