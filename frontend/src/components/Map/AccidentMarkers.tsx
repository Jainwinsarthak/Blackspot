import {ScatterplotLayer} from '@deck.gl/layers'; import type {Segment} from '../../types'; export const accidentMarkers=(data:Segment[])=>new ScatterplotLayer<Segment>({id:'accident-markers',data:data.filter(x=>x.historical_accidents>0),getPosition:d=>[d.lon,d.lat],getRadius:d=>Math.min(34,7+d.historical_accidents*3),radiusMinPixels:2,getFillColor:[255,45,85,160],pickable:true});
