import {BarChart,Bar,Cell,ResponsiveContainer,XAxis,YAxis,Tooltip} from 'recharts'; import type {Factor} from '../../types'; import {RISK_COLORS} from '../../config/constants'; import './FeatureImportanceChart.css'; export function FeatureImportanceChart({factors}:{factors:Factor[]}){const data=factors.slice(0,8).map(x=>({...x,feature:x.feature.replace(/_/g,' ')}));const colors=[RISK_COLORS.critical,RISK_COLORS.high,RISK_COLORS.medium,RISK_COLORS.low];return <section className="feature-chart"><h3>Why these roads are risky</h3><ResponsiveContainer width="100%" height={220}><BarChart data={data} layout="vertical" margin={{left:5,right:20}}><XAxis type="number" hide/><YAxis type="category" width={120} dataKey="feature" tick={{fill:'#94a3b8',fontSize:10}}/><Tooltip cursor={{fill:'#ffffff08'}}/><Bar dataKey="importance" radius={[0,4,4,0]}>{data.map((_,i)=><Cell key={i} fill={colors[i%colors.length]}/>)}</Bar></BarChart></ResponsiveContainer></section>}
